{
  "name": "slow-tee",
  "version": "0.0.1",
  "description": "Distribute the input from a ReadableStream into a number of outputs at the pace of the slowest output, avoiding the possible unbounded memory usage of ReadableStream tee().",
  "homepage": "https://github.com/ion1/slow-tee",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Johan Kiviniemi",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.1",
    "typescript": "^5.0.4"
  }
}
